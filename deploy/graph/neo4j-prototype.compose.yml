services:
  neo4j:
    image: neo4j:5.26-community
    container_name: ai-mot-neo4j-prototype
    restart: unless-stopped
    ports:
      - "127.0.0.1:7474:7474"
      - "127.0.0.1:7687:7687"
    env_file:
      - ./neo4j.env
    environment:
      # Docker Desktop on the Pro currently has a ~4 GiB VM memory cap. Keep
      # Neo4j comfortably below it; the prototype graph is small enough for
      # this budget and can be raised later if the Docker VM limit is increased.
      NEO4J_server_memory_heap_initial__size: 1G
      NEO4J_server_memory_heap_max__size: 1536M
      NEO4J_server_memory_pagecache_size: 1536M
      NEO4J_dbms_security_procedures_unrestricted: "gds.*,apoc.*"
      NEO4J_dbms_security_procedures_allowlist: "gds.*,apoc.*"
      NEO4J_PLUGINS: '["apoc", "graph-data-science"]'
    volumes:
      - ./data:/data
      - ./logs:/logs
      - ./import:/var/lib/neo4j/import
      - ./plugins:/plugins
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
